# Changelog — bridge-@SFX@

- @P1@ scaffold: `forge/` for MC @MC@ (Forge @FORGE@, @SINK@ sink), seam
  from `matou-spi` (see `SPI_PIN`), gate `tools/check.sh` (etages 1+2
  green without MC; live @P3@ todo).
- @P2@ content wiring: `ForgeContentCheck` pure E2E (bridge-@REF_SFX@
  pattern), packs from `config/matoubridge/packs.cfg`.

name: check
on: [push, pull_request]
jobs:
  gate:
    runs-on: ubuntu-24.04
    steps:
      # Action pinned by SHA 2026-09-09 (tag v4); Dependabot keeps it fresh.
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: sh tools/check.sh
